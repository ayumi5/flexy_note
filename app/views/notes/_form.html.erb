<%= simple_form_for @note, html: { class: "form-horizontal note" },
  wrapper: :horizontal_form do |f| %>
  <%= f.input :title %>
  <%= f.input :category %>
  <%= f.input :content, label: 'note', input_html: { rows: 8 } %>
  <%= f.input :url, as: :url %>
  <%= f.button :submit, class: 'create-note' %>

<% end %>
